Instructions:

Copy the matrix file topology.txt in LinkStateRoutingProtocol.jar file folder

To run java file in command prompt
Goto the address of the file in command prompt
Type the following command "java -jar LinkStateRoutingProtocol.jar" and press enter OR run the 'runProject.bat' file
The main menu appears. Perform the operations as required

The Zip contains following files:

1. LinkStateRoutingProtocol.jar		(executable jar)
2. runProject.bat			(for running the jar file)
3. topology.txt 			(input matrix file)
4. readme.txt